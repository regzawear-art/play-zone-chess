To use the local Stockfish build, place a browser-compatible Stockfish build here.

Recommended files:
- stockfish.js
- stockfish.wasm (if the JS build expects it)

You can obtain a prebuilt JS+WASM from the official Stockfish releases or a trusted CDN. Once placed here, the worker will load /stockfish/stockfish.js automatically.